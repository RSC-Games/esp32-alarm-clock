# NOTE

The files in this folder (with the sole exception of gen_firmware_image.py) originate from the ESP-IDF project (https://github.com/espressif/esp-idf). While these were written specifically for imaging ESP32 devices, it is possible to use them to generate device-agnostic firmware images.